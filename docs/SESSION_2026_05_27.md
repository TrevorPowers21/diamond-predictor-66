# Session 2026-05-27 — Hotfixes, National Seed benchmark, portal update

## TL;DR

Five PRs shipped, all merged to main and deployed. Prod data refreshed for
hitter D1 transfer precomputes, National Seed table populated, and today's
Verified Athletics portal CSVs imported. Next session: shift back to scouting
reports + AI scrape (the work paused 2026-05-26 mid-pitch-shape Q&A).

---

## PRs shipped today

| PR | Branch | Summary |
|---|---|---|
| #48 | `hotfix/value-input-analytics-export` → `staging` | Actual Value override + PDF cleanup + National Seed benchmark |
| #49 | `staging` → `main` | Promote #48 |
| #50 | `hotfix/d1-only-emulate-dropdown` → `staging` | D1-only filter + searchable Popover/Command combobox |
| #51 | `hotfix/emulate-dropdown-polish` → `staging` | Total-WAR sort + inline WAR mini-stat + WhatsNew 2026-05-27 release |
| #52 | `staging` → `main` | Promote #50 + #51 |

---

## Code changes by file

### Actual Value override (PR #48)

- `src/pages/team-builder/types.ts` — added `nil_value_overridden?: boolean` to `BuildPlayer`.
- `src/pages/team-builder/helpers.ts` — round-trip `nilValueOverridden` in
  `parseBuildPlayerMeta` + `serializeBuildPlayerMeta`.
- `src/pages/team-builder/hooks/useLoadBuild.ts` — load the flag from meta.
- `src/pages/TeamBuilder.tsx` — pass the flag into `serializeBuildPlayerMeta`.
- `src/pages/team-builder/hooks/useTeamBuilderSimulation.ts` — new
  `projectedBudgetShareForPlayer` is the single source of truth used by both
  `effectiveNilForPlayer` (budget math) and `projectedBudgetValue` (display
  column). 100% of budget always distributes. Overrides redistribute the
  freed budget proportionally. TWP behavior preserved (each row counts).
- `src/pages/team-builder/PlayerTableRow.tsx` — input handler accepts `0`,
  sets `nil_value_overridden = true`. Empty input clears the override.

### PDF cleanup (PR #48)

- `src/lib/pdfGenerator.ts` — stripped "* Assuming player returns to TEAM"
  footnote from both hitter + pitcher PDF projected stats sections, and
  removed the trailing `*` from the section titles.

### National Seed benchmark (PR #48 + #50 + #51)

- `src/hooks/useTeamWarSnapshots.ts` — added `national_seed_rank` to type;
  new `useNationalSeedBenchmark(season, range)` returning min/max/median per
  metric across top-8 (or top-9-16); new `useAllTeamSnapshots(season)` for
  the Emulate dropdown with JUCO/CC filter + total-WAR sort.
- `src/pages/team-builder/tabs/AnalyticsTab.tsx` — replaced National Champion
  row with National Seed (1-8) range row. Added Emulate combobox (Popover +
  Command) with searchable input, name/conf label + inline `XX.X WAR`
  mini-stat. `cursor-pointer` added to `CommandItem`s.
- `supabase/migrations/20260527000000_team_war_snapshots_national_seed_rank.sql`
  — adds `national_seed_rank int null` column + partial index.
- `supabase/queries/seed_national_seed_rank_2026.sql` — 16 UPDATEs keyed by
  exact `source_team_id` for 2026 seeds.

### WhatsNewModal (PR #51)

- `src/components/WhatsNewModal.tsx` — `STORAGE_KEY` bumped to `v3`; added
  2026-05-27 release entry. Pre-existing em dash on a 2026-05-24 detail
  line replaced with a comma per `feedback_no_em_dashes_modal.md`.

---

## Prod runbook executed

1. **National Seed SQL** (`supabase/queries/seed_national_seed_rank_2026.sql`)
   — schema migration + 16 seed assignments. Verified 16/16 matched on prod.
2. **D1 hitter transfer precompute re-run** for all 8 customer teams
   (Arizona State, Arkansas, Florida Atlantic, Georgia, Kansas, Penn State,
   Stetson, TCU). Fixed Reese-Moore-class staleness on pre-tier-PA rows.
3. **D1 pitcher transfer precompute audit** — found 46,706 healthy rows
   across all customer teams. No rerun needed.
4. **Transfer portal CSV import** — `Commits_2026-05-27.csv` (1 committed),
   `transfers_2026-05-27.csv` (137 matched, 16 unmatched), `withdraws_2026-05-27.csv`
   (3 withdrawn). Pull Air CSV still in inbox, silently skipped (Class Data
   importer not wired).

---

## Prod data state at session end

- **Hitter D1 transfer precomputed rows:** all 8 customer teams refreshed
  with tier-PA model. Reese Moore + any other sparse-PA returner reads
  correctly (utility tier, 85 PA, oWAR ~0.5).
- **Hitter JUCO transfer precomputed rows:** 7 customer teams populated
  (from yesterday's runbook), Arizona State added today.
- **Pitcher D1 transfer precomputed rows:** all 8 customer teams populated
  (from 2026-05-24 + 25 runs). Tier-IP applied. 46,706 rows total.
- **Pitcher JUCO transfer precomputed rows:** 7 customer teams populated
  (from yesterday's runbook).
- **Returner regular rows (cross-team):** all clean for hitters + pitchers
  D1 + JUCO.
- **National Seed table:** 16 teams seeded (1-16) for 2026 season.
- **Portal status:** updated today via VA daily CSV drop.

---

## Open items (priority order for next sessions)

### Top priority — SCOUTING REPORTS + AI scrape

Picked up from 2026-05-26 session pause. Reference docs:
- `docs/SCOUTING_REPORT_FRAMEWORK_2026_05_26.md` — master tier thresholds +
  archetypes + voice rules + Trevor quotes.
- `~/.claude/projects/-Users-danielleogonowski/memory/project_scouting_framework_master_2026_05_26.md`
  — inlined memory mirror.
- `~/.claude/projects/-Users-danielleogonowski/memory/project_scouting_session_2026_05_26.md`
  — session work tracker, open items.

Open scouting items captured from that session:
- Pitch-shape distributions Q&A — paused mid-stream. Still need: Slider,
  Sweeper, Curveball, Gyro Slider, Changeup, Cutter, Splitter empirical
  tier thresholds.
- `src/lib/scoutingArchetypes.ts` — known bug: Stevenson detection uses
  badChase (wrong, should be plusChase). Bregman archetype missing entirely.
- AI scrape — the actual generation pipeline. Reference
  `project_ai_scouting_reports.md` for cost analysis (~$25/yr via Haiku
  4.5 for 10k reports). Edge function `generate-scouting-report` was a
  planned next step.
- AI Scouting Reports table migration (`ai_scouting_reports`) not yet built.

### Medium priority

- **D1 307 null p_era audit** — small population, mostly looks like "no PM
  row" cases. Confirm not a code-skip bug.

### Low priority / queued

- **Transfer portal equation calibration** — SEC vs Big 12 gap feels wide
  for JUCO transfers. Pre-existing, captured in earlier session notes.
- **JUCO regular variant SLG corruption** — was flagged 2026-05-26; may be
  resolved by yesterday's refresh, worth verifying.

---

## Workflow reminders (from memory)

- WhatsNewModal release notes: **NEVER use em dashes (—)**. Coach voice is
  short, scannable, conversational. (`feedback_no_em_dashes_modal.md`)
- Don't auto-merge PRs — Trevor handles GH UI merges himself.
- Send SQL to paste in Supabase SQL editor, not TS scripts I execute.
- TruMedia CSV imports run via `npm run import:prod` directly (skip
  staging unless importer code changed).
- Push to staging first, then PR → main. Never feature → main directly.
- Plaintext for paths/URLs the user needs to copy.
