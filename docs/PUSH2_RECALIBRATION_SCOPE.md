# Recalibration — GROUNDED SCOPE (verified against the code 2026-08-08)

Executable checklist for the WAR recalibration + composite display swap. Supplements
`docs/PUSH2_RECALIBRATION_PLAN.md` with the actual file:line map (recon 2026-08-08) and the corrections it
surfaced. Branch off `main`. Staging first; ONE prod push on "prod, now?".

## ⚠ Three corrections the recon surfaced (read before scoping effort)
1. **pWAR recalibration does NOT live in `war.ts`.** `war.ts` `computePWar` (5.5/2.5/10) is effectively DEAD for
   production — only `TeamProfilePage.tsx` + tests reach it. The REAL production/precompute pWAR uses the
   `pwar_*` weights **7.11 / 1.5 / 10** in TWO copies: `src/lib/pitchingEquations.ts:239-241` (frontend) and the
   edge fn `supabase/functions/process-precompute-jobs/index.ts:519`. Neither imports `war.ts`. **So recalibrating
   `RUNS_PER_9`/`PITCHER_REPLACEMENT_PER_9IP` in war.ts will NOT move stored pWAR** — the pWAR flip must target
   those two `pwar_*` weight sets. Divergence is 3-way, not 2-way.
2. **No bat/glove/legs breakdown UI exists.** Every `o_war` display site is a HEADLINE candidate — there's no
   "component" display to preserve. Simpler logic, but ~15 files to swap.
3. **`pickHitterWar`/`pickPitcherWar`/`total_hitter_war` frontend helpers don't exist yet** — must be created;
   only the SQL migration + one edge-fn comment reference `total_hitter_war` today.

## DECISIONS to lock before code moves
- **(D1) The single reconciled pWAR constant set.** Production is currently `r/9 7.11, repl 1.5, rpw 10`. Research
  D1 = `r/9 6.76, repl ≈2.48, rpw 13.1`. Confirm we flip the `pwar_*` weights (both copies) to the research set.
  (Note: 7.11→6.76 lowers run env, 1.5→2.48 raises replacement — both shrink pWAR, on top of rpw 10→13.1.)
- **(D2) Re-run dRS engine FULL-season for the player store.** Composite is currently o_war=full but
  d_war/bsr_war=regular (dRS `is_regular_season` filter). Drop the filter for the player-store aggregates;
  keep a regular split for `team_war_snapshots`. Confirm.

## STEP 1 — Centralize oWAR (7 inlined copies → import war.ts constants)
Each inlines `runsPerPa=0.13 / (pa/600)*25 / /10`:
- `src/lib/playerCalcs.ts:21-26` (the shared `computeOWarFromWrcPlus` — make IT import the constants)
- `src/lib/transferProjection.ts:122-126`
- `src/lib/buildTransferProjectionInputs.ts:394-398` (default param `runsPerPa=0.13`)
- `src/lib/depthRoles.ts:295-300`
- `src/pages/TeamBuilder.tsx:2673-2677` (MIXED — also imports the helper at :32 but re-inlines here)
- `src/pages/team-builder/hooks/useTeamBuilderSimulation.ts:1116-1120` (MIXED — imports at :5, uses at :693/:1522, re-inlines here)
- `supabase/functions/process-precompute-jobs/index.ts:896-901` (edge fn — deploy separately)

Import-only sites (no math change): projectEffective.ts, effectiveProjection.ts, ReturningPlayers.tsx,
PlayerProfile.tsx, TeamProfilePage.tsx.

## STEP 2 — Flip the constants (war.ts) to D1
`src/savant/lib/war.ts:9-13`: RUNS_PER_WIN 10→**13.1**, RUNS_PER_PA 0.13→**0.174**, RUNS_PER_9 5.5→**6.76**,
REPLACEMENT_RUNS_PER_600PA 25→**26.2** (express as 2.0 wins/600PA · rpw), PITCHER_REPLACEMENT_PER_9IP 2.5→**≈2.48**.
Update the `WAR_RECALIBRATION_TODO` block (:15-37) to "done". (These move oWAR once centralized; pWAR moves via D1/D2.)

## STEP 3 — pWAR flip (the REAL path, per correction #1)
- `src/lib/pitchingEquations.ts:239-241` — `pwar_r_per_9 7.11→6.76, pwar_replacement_runs_per_9 1.5→2.48, pwar_runs_per_win 10→13.1`.
- `supabase/functions/process-precompute-jobs/index.ts:519` — same three, same values.
- Consider centralizing these into ONE shared constant so they can't desync again (currently 2 hand-kept copies).
- `war.ts` `computePWar` 5.5/2.5 → reconcile to the same D1 set (or delete/mark dead — only TeamProfilePage+tests use it).

## STEP 4 — Composite SQL `/10.0 → /13.1`
`supabase/migrations/20260806_composite_war_and_refresh.sql`: line **30** (`sum(drs_floor)/10.0`) and line **36**
(`wsb_runs_reg/10.0`) → `/13.1`. Ship a new migration (don't edit the applied one). Note: this only sums the
additive buckets; o_war/p_war are regenerated by the re-precompute, not here. Also do D2 (full-season d/bsr) here.

## STEP 5 — Display swap (o_war → total_hitter_war)
- Create `pickHitterWar` (= total_hitter_war = o+d+bsr) + `pickPitcherWar` (= p_war) — analogs of
  `pickHitter/PitcherMarketValue` in `src/lib/twpMarketValue.ts`.
- Swap the ~15 headline sites (all headline, no component to protect): PlayerComparison.tsx:361-363,
  useTargetBoard.ts:140/154/162/174, gm/useGmTargetBoard.ts + loadGmBuildRoster.ts, ScoutingReport.tsx,
  ReturningPlayers.tsx, HighFollowList.tsx, TransferPortal.tsx, CompareTab.tsx, PlayerHub.tsx, PlayerProfile.tsx,
  AdminDashboard.tsx, NilValuations.tsx, PlayerTableRow.tsx, TeamBuilder.tsx, TargetBoardSubtab.tsx, AnalyticsTab.tsx.
- BOTH paths: stored-read AND live-compute (`computeOWarFromWrcPlus` in useTeamBuilderSimulation ~:693/:1522 —
  add d_war+bsr_war).

## STEP 6 — Tests (update the literals that will break)
- `src/lib/playerCalcs.test.ts:31,35` — hardcoded `2.5`, `4.84` → recompute for 13.1/new repl.
- `src/lib/storedVsLive.test.ts:39-41,48,54-55,58-88` — local literal constants + the `inlineOWar` parity guard → update.
- `src/savant/lib/war.test.ts` — mostly symbolic (auto-follows); line 214 safe.
- **GAP:** the real pWAR weights (7.11/1.5/10) + the SQL `/10.0` have NO parity test — flips pass `npm test` silently.
  Add a guard test for the pWAR weights + a value check for the composite SQL.
- `CLAUDE.md` "Team Builder — WAR Benchmarks" documents the OLD formulas — update alongside.

## STEP 7 — Re-precompute + reseed + repoint (staging → verify → prod)
- ONE staging re-precompute (D1 path, every customer team); SNAPSHOT `player_predictions` between so the impact
  story is attributable. Regenerates stored o_war/p_war at 13.1; `refresh_composite_war()` repopulates total_hitter_war.
- Verify: 250-PA hitter ~2.6 WAR; pitchers still > hitters; identity total_hitter_war = o+d+bsr holds; pages load.
- Reseed `team_war_snapshots` (`supabase/queries/seed_team_war_snapshots_2025.sql`) on the new totals — REGULAR-season
  (the one date-filtered run). Repoint market value / projected budget reads o_war → total_hitter_war.
- Prod also needs: the SEQUENCE-COLUMN replay (Step 0) + the Push-1-style prod DDL-as-owner steps.

## Push sequence
Centralize + flip + swap on a branch → `npm test` → deploy edge fn to staging + re-precompute staging + verify +
load pages → staging→main PR → on "prod, now?": deploy edge fn prod, run SQL /13.1 migration, re-precompute prod,
reseed snapshots, repoint. Changelog acknowledges: dWAR+bsrWAR added, recalibrated to D1 (~-23%), composite displayed.
